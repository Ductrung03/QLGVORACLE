/**
 * Sidebar Component for Teacher Management
 *
 * This component wraps the shared Sidebar component and can be customized
 * for the teacher management section if needed.
 */

"use client";

import SharedSidebar from '@/shared/layouts-components/sidebar/sidebar';

const Sidebar = () => {
  return <SharedSidebar />;
};

export default Sidebar;
